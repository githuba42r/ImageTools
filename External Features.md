create a browser plugin that will capture screenshots (full page, visible page, selected area) of the current webpage an send it to the Image Tools.

the image tools will need an oauth2 style authorization process for the browser plugin to authenticate the browser to an account in image tools.

the image tools about should include a button to copy a registration URL that can be pasted into the browser plugin to commence the enrolment process.

create a browser plugin for Firefox and chrome


create an android app, that will provide a share to Image Tools, the will need to be an oauth2 style authorzation to link the android app to the users image  tools instance, this is best performed with the app scanning a QR code that the image tools will display on the about settings to generate the link.

the qr code will include the url to the image tools instance and a share secret that will be used to authenicate image uploads to the service.
