# ImageTools Deployment Configuration
# Copy this file to .env and customize the values

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Application name (displayed in browser auth prompt)
APP_NAME=Image Tools

# =============================================================================
# BASIC AUTHENTICATION (Simple Mode)
# =============================================================================
# Quick setup: Provide username and password for a single user
# The htpasswd file will be created automatically on first run
# Will NOT overwrite existing htpasswd file in mounted volume

DFLT_USERNAME=admin
DFLT_PASSWORD=changeme123

# =============================================================================
# IMAGETOOLS BACKEND SETTINGS
# =============================================================================

# Session expiry (days)
SESSION_EXPIRY_DAYS=7

# Maximum images per session
MAX_IMAGES_PER_SESSION=5

# Maximum upload size (MB)
MAX_UPLOAD_SIZE_MB=20

# =============================================================================
# OPENROUTER OAUTH (Optional - for AI features)
# =============================================================================
# Get credentials at: https://openrouter.ai/
# Leave empty to disable AI features

OPENROUTER_CLIENT_ID=
OPENROUTER_CLIENT_SECRET=
OAUTH_REDIRECT_URL=http://localhost/api/v1/openrouter/callback
