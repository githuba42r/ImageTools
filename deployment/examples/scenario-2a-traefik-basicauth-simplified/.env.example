# ImageTools - Scenario 2A: Traefik + Basic Auth (Simplified)
# 
# ⚠️ This is the Simplified variant without internal authentication.
# For production deployments, use Scenario 2B (Hardened) instead.

# Basic Auth Configuration
# Generate with: htpasswd -nb admin yourpassword
# Or: docker run --rm httpd:alpine htpasswd -nb admin yourpassword
# Important: Escape $ signs by using $$ in docker-compose
BASIC_AUTH_USERS=admin:$$apr1$$hash$$here

# Optional: OpenRouter OAuth (if using AI features)
OPENROUTER_CLIENT_ID=
OPENROUTER_CLIENT_SECRET=
